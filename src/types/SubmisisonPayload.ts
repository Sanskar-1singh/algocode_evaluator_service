 type SubmisisonPayload={
    code:string,
    language:string,
    inputCase: string,
    outputCase: string,
    userId:string,
    submissionId:string//from where i am sending this???
}

export default SubmisisonPayload;